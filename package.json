{
  "name": "redux-fractal",
  "version": "1.0.0-0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "NODE_PATH=src jest",
    "flow": "flow",
    "flow:coverage": "flow-coverage-report -i 'src/**/*.js' -x 'test/**' -t html -t json -t text --strict-coverage",
    "build": "babel src/ -d dist/",
    "prepublish": "npm run -s build"
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "redux": "^4"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "@babel/preset-flow": "^7.0.0",
    "babel-core": "^7.0.0-bridge.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.7.0",
    "eslint-plugin-flowtype": "^3.0.0",
    "flow-bin": "^0.83.0",
    "flow-coverage-report": "^0.6.0",
    "flow-typed": "^2.5.1",
    "jest": "^23.6.0",
    "jest-junit": "^5.2.0",
    "redux": "^4.0.1"
  },
  "jest": {
    
  }
}
